import { Typography } from 'antd'
import React from 'react'

function TokenExpiredPage() {
	return (
		<div>
			<Typography.Title level={2}>Expired Token</Typography.Title>
			<Typography.Text>PLease generate new token</Typography.Text>
		</div>
	)
}

export default TokenExpiredPage
